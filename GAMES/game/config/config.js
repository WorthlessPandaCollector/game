const config = {
    classNames: {
        MageClassName: "mage",
        WarlockClassName: "warlock",
        WarriorClassName: "warrior",
        ArcherClassName: "archer"
    }
}

module.exports = config;

